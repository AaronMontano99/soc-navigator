fastapi>=0.110
uvicorn[standard]>=0.29
pyyaml>=6.0
pydantic>=2.6

# Optional: enables live LLM chat in the AI Investigation Assistant
# (falls back to deterministic templates if not installed / no API key set)
# anthropic>=0.40
